import React from 'react';
import './styles.module.css'; // Importa o CSS

const RatingsSection = () => (
  <div className="profile-section">
    <h3>Ratings</h3>
    <p>No ratings available.</p>
  </div>
);

export default RatingsSection;
